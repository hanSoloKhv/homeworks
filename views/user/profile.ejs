<%- include('../partials/layout-start', {title: title}) %>

<h1><%= title %></h1>

<div class="row">
    <div class="col-sm-8">
        <div class="card">
            <div class="card-body">

                <h5 class="card-title">Name: <%= user.login %></h5>
                <p class="card-text">Id: <%= user.id %></p>

                <div class="text-right">
                    <form action="/user/signout" method="POST" class="d-inline">
                        <button class="btn btn-sm">
                            Выход
                        </button>
                    </form>
                </div>

            </div>
        </div>
    </div>
</div>

<%- include('../partials/layout-end') %>